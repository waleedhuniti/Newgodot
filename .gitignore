# Godot 3.x - import cache and per-file import artifacts. Regenerated by running
# `godot3 --path . --editor --quit` once (slow - see docs/TECHNICAL_PLAN.md §3) -
# not worth committing given how much of art/ isn't used by any scene yet.
.import/
*.import
*.material
export.cfg
export_presets.cfg
*.translation

# API keys and secrets
.env

# OS metadata
.DS_Store
Thumbs.db

# Editors
.idea
*.iml
*.swp
